spring:
  application:
    name: gn-authorizing-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:9999
      name: geonetwork
server:
  port: 9998
security:
  oauth2:
    client:
      client-id: first-client
      client-secret: noonewilleverguess