spring:
  application:
    name: gn-ogc-api-records-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:9999
      name: geonetwork
      username: gn
      password: gn
server:
  port: 9991
